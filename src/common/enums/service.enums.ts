export enum ServiceType {
  ONLINE = 'online',
  OFFLINE = 'offline',
}

export enum ServiceClassType {
  PRIVATE = 'private',
  GROUP = 'group',
  WORKSHOP = 'workshop',
  CORPORATE = 'corporate',
}
