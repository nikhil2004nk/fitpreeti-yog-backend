export enum LeadInterestedIn {
  ONLINE = 'online',
  OFFLINE = 'offline',
  CORPORATE = 'corporate',
}

export enum LeadPreferredClassType {
  PRIVATE = 'private',
  GROUP = 'group',
  WORKSHOP = 'workshop',
}

export enum LeadStatus {
  NEW = 'new',
  CONTACTED = 'contacted',
  INTERESTED = 'interested',
  NEGOTIATING = 'negotiating',
  CONVERTED = 'converted',
  REJECTED = 'rejected',
  NOT_RESPONDING = 'not_responding',
}

export enum LeadPriority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum LeadExperienceLevel {
  BEGINNER = 'beginner',
  INTERMEDIATE = 'intermediate',
  ADVANCED = 'advanced',
}

export enum LeadPreferredTimeSlot {
  MORNING = 'morning',
  AFTERNOON = 'afternoon',
  EVENING = 'evening',
}

export enum LeadActivityType {
  CALL = 'call',
  EMAIL = 'email',
  WHATSAPP = 'whatsapp',
  MEETING = 'meeting',
  NOTE = 'note',
  STATUS_CHANGE = 'status_change',
}
